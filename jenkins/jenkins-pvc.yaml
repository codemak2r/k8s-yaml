apiVersion: v1
kind: PersistenVolumeClaim
metadata:
  name: jenkins-pvc
  labels:
    env: devops
spec:
  storageClassName: jenkins-storage
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi