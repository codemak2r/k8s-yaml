apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jenkins-pvc
  labels:
    env: devops
spec:
  storageClassName: jenkins-storage
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi